# TrendRadar 核心依赖
# 安装: pip install -r requirements.txt
# Dev:  pip install -e ".[dev]"
# Free-threaded: pip install -e ".[free-threaded]"

aiohttp>=3.9
feedparser>=6.0
pyyaml>=6.0
pyahocorasick>=2.0
zstandard>=0.23; platform_system != 'Windows'
