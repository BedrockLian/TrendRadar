# TrendRadar 依赖清单
# 安装: pip install -r requirements.txt
# 或: pip install -e .（editable install，推荐）

aiohttp>=3.9
feedparser>=6.0
pyyaml>=6.0
pyahocorasick>=2.0
zstandard>=0.23; platform_system != 'Windows'
charset-normalizer>=3.0
